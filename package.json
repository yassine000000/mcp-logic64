{
    "name": "logic64-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev:studio": "npm run dev --workspace=apps/studio",
        "dev:kernel": "npm run dev --workspace=apps/kernel"
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}